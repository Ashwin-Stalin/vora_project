slam_toolbox:
  ros__parameters:
    use_sim_time: true
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan
    mode: mapping
    map_update_interval: 1.0
    resolution: 0.05
    max_laser_range: 12.0 # Matches your Gazebo LiDAR setting
